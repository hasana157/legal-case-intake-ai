# =============================================================================
# api/requirements.txt
# Python dependencies for the FastAPI backend.
# Run: pip install -r requirements.txt
# =============================================================================

# ── Core API ─────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.4.0
python-dotenv>=1.0.0
pytest>=7.4.0

# ── Milestone 2: LLM Extraction ──────────────────────────────────────────────
# We use Groq's fast, free-tier-friendly API for JSON extraction.
groq>=0.11.0

# ── Milestone 2: Retry logic for LLM calls ───────────────────────────────────
tenacity>=8.2.3

# ── Milestone 3: RAG Retrieval ───────────────────────────────────────────────
qdrant-client>=1.7.0
sentence-transformers>=2.3.0
torch>=2.0.0
huggingface_hub>=0.23.0
datasets>=2.18.0,<4.0.0
langchain-text-splitters>=0.2.0
tqdm>=4.66.0

# ── Milestone 6: Rate limiting & load testing ─────────────────────────────────
slowapi>=0.1.9
httpx[http2]>=0.27.0
requests>=2.32.0
